# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Google Books API (server-only)
GOOGLE_BOOKS_API_KEY=your-google-books-api-key

# OpenAI API (server-only, for embedding generation)
OPENAI_API_KEY=sk-your-openai-api-key

# Supabase Service Role Key (server-only, for embedding writes — never expose to client)
# Find at: Supabase Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
